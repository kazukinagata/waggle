{
  "description": "Injects standing Waggle task-protocol guidance into the session (SessionStart). Delegates to hooks/session-guidance.sh; fails open cleanly when CLAUDE_PLUGIN_ROOT is unresolved (e.g. Cowork-Windows). Re-runs after compaction so the guidance survives context compaction.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|compact|resume",
        "hooks": [
          {
            "type": "command",
            "command": "P=\"${CLAUDE_PLUGIN_ROOT:-}/hooks/session-guidance.sh\"; [ -f \"$P\" ] && exec bash \"$P\"; echo '{}'",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
