{
  "manifest_version": "0.3",
  "name": "mcpb-debug-notion-extension-clone",
  "version": "0.0.1",
  "description": "Exact functional clone of providers/notion/extension/ (real Notion API calls — not a mock) but with a different name and no display_name. Used to confirm the prefix-casing fix works end-to-end against real Notion data before applying the breaking change to the real extension.",
  "author": {
    "name": "Kazuki Nagata"
  },
  "server": {
    "type": "node",
    "entry_point": "server/index.js",
    "mcp_config": {
      "command": "node",
      "args": ["${__dirname}/server/index.js"],
      "env": {
        "NOTION_TOKEN": "${user_config.notion_token}"
      }
    }
  },
  "tools": [
    {
      "name": "notion-query",
      "description": "Query a Notion database with server-side filtering, sorting, and pagination. Supports people property filters (Assignee) that the Notion hosted MCP cannot handle. Accepts page_size / start_cursor / filter_properties so the caller can drive pagination and projection on large databases."
    },
    {
      "name": "notion-update-relation",
      "description": "Update a relation property on a Notion page. Supports replace (set exact list) and append (merge with existing, dedup) modes."
    }
  ],
  "user_config": {
    "notion_token": {
      "type": "string",
      "title": "Notion Internal Integration Token",
      "description": "Your Notion internal integration token (ntn_...). Create one at https://www.notion.so/profile/integrations with Read content permission, then connect it to your Waggle page.",
      "sensitive": true,
      "required": true
    }
  },
  "keywords": ["notion", "query", "relation", "waggle", "database", "filter"],
  "license": "MIT",
  "compatibility": {
    "platforms": ["darwin", "win32", "linux"]
  }
}
