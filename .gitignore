node_modules/
dist/
*.tsbuildinfo
.claude/scheduled_tasks.lock
.claude/worktrees/

# Desktop Extension build artifacts — regenerated via `npx @anthropic-ai/mcpb pack .`
*.mcpb

# Debug-extension lockfiles — pure tooling noise, regenerated by npm install
experiments/**/package-lock.json
